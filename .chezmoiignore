*.sw*

.vim/undo/*
.vim/temp/*
.vim/backup/*
.viminfo

# Build artifacts
tags

# Secrets — keep in 1Password, not dotfiles
.anthropic_api_key
.gh_access_token
.jira_api_token
.claude.json*

# Instinct internal helpers — company IP, not for personal repo
.instinct*
.zshrc_instinct

# Per-host or tool-managed, not portable
.zprofile
.tool-versions
.sqlfluff
.yarnrc

# Installer artifacts (managed by their own installers)
.fzf.bash
.fzf.zsh

# This README is for the source repo, not for $HOME
README.md
